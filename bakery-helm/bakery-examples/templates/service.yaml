apiVersion: v1
kind: Service
metadata:
  name: {{ include "reference.fullname" . }}
  labels:
    {{- include "reference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reference.selectorLabels" . | nindent 4 }}
